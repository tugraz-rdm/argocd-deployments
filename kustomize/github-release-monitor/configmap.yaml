apiVersion: v1
kind: ConfigMap
metadata:
  name: github-release-monitor-config
data:
  config.yaml: |
    # GitHub Release Monitor Configuration
    ## poll all configured repositories once per hour for new releases
    interval: 3600
    logLevel: info
